
#pragma clang diagnostic ignored "-Wc++11-extensions"

class automata{

	protected:
	// add a bidemensional int array in the cpp to use whit walk 
	// if extending this class
	int currentIndex  = 1;
	public:
	 int walk(int);



};